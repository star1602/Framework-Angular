import { EjercicioDirective } from './ejercicio.directive';

describe('EjercicioDirective', () => {
  it('should create an instance', () => {
    const directive = new EjercicioDirective();
    expect(directive).toBeTruthy();
  });
});
